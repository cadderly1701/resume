import{t as e}from"./index-lDt8LPzc.js";var t=e();function n(e){let n={a:`a`,em:`em`,hr:`hr`,li:`li`,p:`p`,ul:`ul`,...e.components},{Expand:r,Hero:a,LineDrawing:o,Pull:s,Section:c,TwoLevels:l}=n;return r||i(`Expand`,!0),a||i(`Hero`,!0),o||i(`LineDrawing`,!0),s||i(`Pull`,!0),c||i(`Section`,!0),l||i(`TwoLevels`,!0),(0,t.jsxs)(t.Fragment,{children:[(0,t.jsx)(a,{eyebrow:`Vendor management · Sample 05`,title:`When should fifteen processes share one flow?`,subtitle:`One controller flow instead of fifteen, and the analysis that earned it.`,side:`left`,children:(0,t.jsx)(o,{name:`reusable-orchestration`,alt:`Fifteen strands entering separately, tangling, and gathering into one line.`})}),`
`,(0,t.jsx)(n.p,{children:`How I tested whether fifteen processes genuinely shared a spine before collapsing them into one flow.`}),`
`,(0,t.jsx)(l,{shortMinutes:4,detailMinutes:11}),`
`,(0,t.jsxs)(c,{id:`problem`,title:`The problem`,children:[(0,t.jsx)(n.p,{children:`Each process has its own catalog item, document requirements, approval chain, and downstream behavior in the ERP. The conventional build is fifteen workflows, and nobody would blink at that in a design review.`}),(0,t.jsx)(n.p,{children:`But fifteen was not the final number and everyone knew it. The client could already name the processes that hadn't made the MVP cut, and nobody could say whether a new one would take a sprint or a budget cycle once the project team rolled off.`}),(0,t.jsx)(n.p,{children:`Fifteen workflows makes that future expensive twice over. A sixteenth process means a sixteenth flow, built and tested from scratch. And a change to something shared means touching fifteen flows and hoping the fifteenth got the same treatment as the first.`}),(0,t.jsx)(s,{children:`That drift shows up two years in, when four flows have quietly diverged and nobody remembers which behavior was intentional.`}),(0,t.jsxs)(r,{title:`I wasn't reasoning about that hypothetically`,summary:`The archaeology on the legacy system, and why you can't safely remove behavior you can't explain`,minutes:2,children:[(0,t.jsx)(n.p,{children:`It was the state of the system we were replacing. There was no document describing what the legacy tool did, so we recovered its logic by observing it: opening every form, running every process variant, and writing down what actually happened. Most of what we found was consistent. Some of it wasn't, and for the parts that weren't, nobody could tell us whether the difference was a decision or an accident that had hardened into one.`}),(0,t.jsx)(n.p,{children:`That question has no answer once the people who made the change have moved on, and it's expensive precisely because you can't safely remove behavior you can't explain. Being the person doing that archaeology is a good way to develop opinions about duplicated logic.`}),(0,t.jsx)(n.p,{children:`The fifteen lifecycle processes, for the record: new vendor, one-time payment, address change, category change, payment terms change, contact change, a catch-all other change, three payment method conversions, three against the in-unit ordering system, plus block and unblock.`})]})]}),`
`,(0,t.jsxs)(c,{id:`built`,title:`What I built`,children:[(0,t.jsx)(n.p,{children:`One Controller Flow calling each phase of a shared spine as a subflow in fixed order, each with a declared input and output contract so it can be tested on its own. What varies is a Catalog Item Configuration record that the catalog item points at: which approvals get evaluated, whether an initial task is created and who it routes to, whether pre-integration gating applies, and which integration packages get sent.`}),(0,t.jsx)(n.p,{children:`The work that mattered happened before any flow got built, and it was a question: do these fifteen processes actually share a spine?`}),(0,t.jsx)(n.p,{children:`The test was whether the order of the steps ever changed. Participation and parameters can vary safely, sequence can't. Fourteen fit a candidate spine cleanly. One didn't, and that seam got absorbed inside a subflow rather than branched in the controller.`}),(0,t.jsxs)(r,{title:`The test I used, and how I walked all fifteen`,summary:`Why 'can they be forced to share one' is the wrong question, and the candidate spine they were checked against`,minutes:2,children:[(0,t.jsx)(n.p,{children:`Not "can they be forced to share one." Fifteen things can always be forced into one flow if you're willing to write enough conditionals. The test I used was whether the order of the steps ever changed. If every process moves through the same phases in the same sequence, and the only variation is which phases participate and with what parameters, then one flow is the honest description of the family and fifteen flows are fifteen copies of it.`}),(0,t.jsx)(s,{children:`If the sequence itself varies, one flow is a lie you tell with nested if statements.`}),(0,t.jsx)(n.p,{children:`So I walked all fifteen against a candidate spine: request arrives, configuration and reference data load, an approver list gets built, an optional fulfillment task is created and routed, approvals run, an optional gate confirms readiness, zero or more packages go to the ERP, then confirmation and closure. Fourteen fit cleanly. One had genuinely process-dependent sequencing.`})]}),(0,t.jsxs)(r,{title:`What the configuration record actually controls`,summary:`Four flag groups, and the routing case that convinced me the model held`,minutes:2,children:[(0,t.jsx)(n.p,{children:`Every catalog item governed by the controller points at a Catalog Item Configuration record, and its flags decide behavior:`}),(0,t.jsxs)(n.ul,{children:[`
`,(0,t.jsx)(n.li,{children:`Which approval types get evaluated, including whether QA approval applies and how many organizational approvals the hierarchy walk has to produce.`}),`
`,(0,t.jsx)(n.li,{children:`Whether an initial task is created and who it routes to. Options are none, the vendor maintenance queue, the Managed Sourcing compliance queue, or Dynamic Domain, which routes on the category code's domain and, for General domain items, on whether the vendor falls in the in-unit ordering system's range. That's routing decided by data rather than by a hard-coded branch, and it's the case that convinced me the model held.`}),`
`,(0,t.jsx)(n.li,{children:`Whether pre-integration gating applies. The gate fires on unresolved approval comments, a manual on-premise update flag, unconfirmed cloud ERP mandatory fields, or a sync mismatch between the two platforms, and holds the request until a fulfiller closes it complete.`}),`
`,(0,t.jsx)(n.li,{children:`Which integration packages get sent. Most changes are a single Update. New vendor and one-time payment create a shell record to obtain an ID and send a Create. A replacement sends that Create, then a second package blocking the old vendor with the new ID noted on it. Some processes skip integration entirely.`}),`
`]}),(0,t.jsx)(n.p,{children:`A Data Policy on the catalog item makes the configuration field visible and mandatory only when the item's flow is the controller, so nothing governed by the controller can be saved without one.`})]}),(0,t.jsxs)(r,{title:`Where the spine wasn't clean`,summary:`The one process with genuinely process-dependent sequencing, and why it didn't become a branch in the controller`,minutes:2,children:[(0,t.jsx)(n.p,{children:`Cost centre updates for the in-unit ordering system need a fulfillment task, and where it belongs in the sequence genuinely differs by process. For a clean new vendor it goes after successful integration. For block, it has to be sent before the block package.`}),(0,t.jsx)(n.p,{children:`Branching the controller on process type would have solved that, and it's exactly the sprawl I was avoiding. It went into the post-integration subflow instead, with process type and the eligibility result passed in as inputs, and one reusable check evaluating the selected vendor plus the replacement vendor where one exists. Evaluated once per execution and held in a flow variable, with an idempotency check so a resumed flow can't create the task twice.`}),(0,t.jsx)(n.p,{children:`That's the argument for the pattern in miniature. Sequencing had to move for a handful of processes, and it was one story against one subflow. In the fifteen-flow build it's a separate change to each of them, a separate test for each, and a standing chance that one which should have changed didn't.`}),(0,t.jsx)(n.p,{children:`A diagram of a controller flow with configuration-driven branching would have gotten polite nods and nothing else. This group needed to see it, so I built the controller end to end in a personal instance, then changed one flag in front of them and let them watch the same flow behave like a different process. It stopped being abstract the moment they could point at the record causing the difference.`})]})]}),`
`,(0,t.jsxs)(c,{id:`why`,title:`Why it was the right call`,children:[(0,t.jsx)(n.p,{children:`The client said they'd keep adding processes and couldn't promise developer capacity to add them. A sixteenth process here becomes a catalog item, a configuration record, and a variable set: a configuration exercise with a testing cycle, not a development cycle, and within reach of the data admin group that already owns the rest of the configuration.`}),(0,t.jsx)(n.p,{children:`And because approval semantics, retry and escalation, and reject-with-reset behavior all live once, consistency stops being something you enforce in code review and becomes structural.`}),(0,t.jsx)(s,{children:`I didn't pick the pattern because it's elegant, I picked it after checking, and if the check had gone the other way I'd have written fifteen flows and defended that instead.`})]}),`
`,(0,t.jsxs)(c,{id:`wrong`,title:`When this would be the wrong call`,children:[(0,t.jsx)(n.p,{children:`Don't build this if the processes don't genuinely share a spine. If the sequence varies rather than the participation, one controller becomes a flow full of conditionals nobody can read, and you've paid for abstraction while still getting duplication, just harder to see. Don't build it for five processes either, or for a catalog that's finished, because the whole payoff is in the sixteenth process.`}),(0,t.jsxs)(r,{title:`And don't build it for a team that would rather read fifteen simple flows`,summary:`A legitimate preference, and the question that actually decides it`,minutes:1,children:[(0,t.jsx)(n.p,{children:`That preference is legitimate and I'd honor it. Debugging a configuration-driven controller means reading a flow and a data record at the same time, and the flow alone never tells you what happened. When something misroutes, the cause might be the configuration flag, the category code, the hierarchy record, or the flow logic.`}),(0,t.jsx)(n.p,{children:`Duplication isn't always the enemy. Fifteen simple flows a support team can each read top to bottom will beat one clever flow only its author understands. The question isn't which is more elegant, it's which one this team can still operate in two years.`})]})]}),`
`,(0,t.jsx)(n.hr,{}),`
`,(0,t.jsx)(n.p,{children:(0,t.jsxs)(n.em,{children:[`My part, specifically: the controller flow with its subflow call contracts, the configuration-driven branching, and the routing behavior. The sequencing refinement described above came out of the delivery cycle rather than the proof of concept. The development team implemented and extended it from there. Engagement status is on the `,(0,t.jsx)(n.a,{href:`/vendor-management`,children:`project page`}),`.`]})})]})}function r(e={}){let{wrapper:r}=e.components||{};return r?(0,t.jsx)(r,{...e,children:(0,t.jsx)(n,{...e})}):n(e)}function i(e,t){throw Error(`Expected `+(t?`component`:`object`)+" `"+e+"` to be defined: you likely forgot to import, pass, or provide it.")}export{r as default};