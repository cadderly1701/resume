import{t as e}from"./index-CUcsWBF9.js";var t=e();function n(e){let n={a:`a`,code:`code`,em:`em`,hr:`hr`,p:`p`,...e.components},{Expand:r,Hero:a,LineDrawing:o,Pull:s,Section:c,TwoLevels:l}=n;return r||i(`Expand`,!0),a||i(`Hero`,!0),o||i(`LineDrawing`,!0),s||i(`Pull`,!0),c||i(`Section`,!0),l||i(`TwoLevels`,!0),(0,t.jsxs)(t.Fragment,{children:[(0,t.jsx)(a,{eyebrow:`Vendor management · Sample 04`,title:`How do you design a data architecture when there is no data dictionary?`,subtitle:`Reverse-engineering two ERP schemas mid-migration, and negotiating the data loop across teams that didn't report to me.`,side:`left`,children:(0,t.jsx)(o,{name:`data-architecture`,alt:`A browser window reading 404, with the words error, documentation not found written beneath it.`})}),`
`,(0,t.jsx)(n.p,{children:`No data dictionary, two live ERP schemas that disagree, and a loop other teams owned.`}),`
`,(0,t.jsx)(l,{shortMinutes:3,detailMinutes:12}),`
`,(0,t.jsxs)(c,{id:`problem`,title:`The problem`,children:[(0,t.jsx)(n.p,{children:`There was no data dictionary and no technical guides from their team. I had to learn both ERP schemas well enough to map them, from the data itself and the people who used the fields daily.`}),(0,t.jsx)(n.p,{children:`Both platforms were live and would stay that way past go-live, neither instance was authoritative for the full set of vendors, and the two didn't agree on identifiers.`}),(0,t.jsx)(n.p,{children:`The loop that carried a vendor out and back ran mostly outside ServiceNow, across teams that didn't report to me.`}),(0,t.jsx)(r,{title:`Why the migration made it worse`,summary:`Two live platforms, no authoritative instance, and every segment of the loop owned by somebody else`,minutes:1,children:(0,t.jsx)(n.p,{children:`Both platforms were live and would stay that way past go-live. Some vendors existed only on the on-premise platform, some on both, neither instance was authoritative for the full set, and the two didn't agree on identifiers. Meanwhile the data loop ran mostly outside ServiceNow, out through a transport layer to the ERP and back around through the warehouse. Every segment but the first belonged to a different team, none of whom reported to me, most of whom owned systems I had no access to.`})})]}),`
`,(0,t.jsxs)(c,{id:`built`,title:`What I built`,children:[(0,t.jsx)(n.p,{children:`A data schema workbook: every field, its source view, type, length, target table, and target field. That became the contract, and stories referenced it by name instead of restating field lists.`}),(0,t.jsxs)(n.p,{children:[`Vendor extends `,(0,t.jsx)(n.code,{children:`core_company`}),`, Vendor Contact went custom and read-only, the client's user fields went onto `,(0,t.jsx)(n.code,{children:`sys_user`}),` as dictionary entries only, and every reference data set shares one Choice table with a Choice Type discriminator.`]}),(0,t.jsx)(n.p,{children:`The org hierarchy is seven tiers deep, so the import processes top down, parents before children. Each import runs as a delta into a staging table, then coalesces in a fixed order: ServiceNow sys_id if we have one, then the ERP ID, then create. When a lookup finds no match, the record relates to a known "Unmatched Data" record rather than nulling out, guessing at the nearest value, or failing the row.`}),(0,t.jsx)(n.p,{children:`Outbound, each operation has its own JSON payload shape and one operation goes per invocation, after a shell record is created to obtain an ID for the round trip. The subflow then checks that the expected change actually came back.`}),(0,t.jsxs)(r,{title:`Rebuilding the schemas from the data, and deciding OOB versus custom`,summary:`Where the field list came from, the sequencing rule that fell out of it, and the rule of thumb for extending a platform table`,minutes:3,children:[(0,t.jsx)(n.p,{children:`I worked from the warehouse views that already exposed both instances, sample extracts, and long sessions with the people who used each field daily, and turned that into a data schema workbook: every field, its source view, type, length, target table, and target field. That became the contract. Stories referenced it by name instead of restating field lists, so a correction landed in one place and everything downstream inherited it.`}),(0,t.jsx)(n.p,{children:`The corrections came from showing rather than asking. A field list in a document got nodded at. The same field list rendered on a form got corrected in about ninety seconds.`}),(0,t.jsx)(n.p,{children:`One sequencing rule fell straight out of it. The org hierarchy is seven tiers deep and parent references only resolve if the parent already exists, so the import processes top down, tier by tier, from the enterprise root to the individual cost centre. Get that order wrong and you get orphans that look like an integration bug.`}),(0,t.jsxs)(n.p,{children:[`Vendor extends `,(0,t.jsx)(n.code,{children:`core_company`}),` with an is-vendor flag. Extending buys you every reference field already pointing at company records, and costs you columns you didn't ask for and a structure you don't fully control. Here the inherited plumbing beat the constraint.`]}),(0,t.jsx)(n.p,{children:`Vendor Contact went custom. Nothing OOB matched the shape, and since the record is mastered elsewhere, inheriting platform behavior would mostly have meant inheriting behavior we'd then switch off. It's read-only by default, and not as a convention: read goes to the fulfiller and data admin roles, write and delete go to admin only, and a non-admin opening one doesn't see a Save button. Auditing is on everywhere. If the ERP is the system of record, the app has to make editing the copy structurally impossible, not merely discouraged.`}),(0,t.jsxs)(n.p,{children:[`For users, I added the client's fields to `,(0,t.jsx)(n.code,{children:`sys_user`}),`, dictionary only: no form layouts, no UI policies, no client scripts. Field creation is safe and reversible, UI exposure is opinionated and deserved its own argument.`]}),(0,t.jsx)(n.p,{children:`Reference data got one custom Choice table with a Choice Type discriminator instead of a table per set. Bank keys, payment methods and terms, withholding tax codes and types, category codes, hierarchy, and market all behave identically: they sync from the warehouse on a schedule and carry ServiceNow-only attributes on top, like tax form requirements or the approval steps hanging off a category code. One table, one import pattern, one set of ACLs, one place to look when something didn't match.`}),(0,t.jsx)(n.p,{children:`Rule of thumb: extend when you want the platform's behavior and can live with its constraints, go custom when the constraint costs more than the behavior is worth.`})]}),(0,t.jsxs)(r,{title:`Identity resolution, and making bad reference data visible`,summary:`The coalesce cascade and its ordering argument, plus the sentinel record I'd defend hardest`,minutes:2,children:[(0,t.jsxs)(n.p,{children:[`Three systems, no shared key. Each import runs as a delta: a `,(0,t.jsx)(n.code,{children:`Last Successful Run`}),` system property per job holds the timestamp, the SQL filters the view against it with an overlap window, and an empty property means pull everything. Records land in an import set staging table, get transformed, then coalesce in a fixed order: ServiceNow sys_id if we have one, then the ERP ID, then create.`]}),(0,t.jsx)(n.p,{children:`That order is the argument: our own key when we already own the row, the ERP key when the ERP owns it, and create only when neither system can claim the record. It's what keeps an on-premise-only vendor from becoming a duplicate the day it finally appears in the cloud platform.`}),(0,t.jsx)(n.p,{children:`When a lookup against the Choice table finds no match, the record relates to a known "Unmatched Data" record rather than nulling out, guessing at the nearest value, or failing the row. A report lists everything pointing at that sentinel, so misses are a queue instead of a mystery.`}),(0,t.jsx)(s,{children:`Nulls and guesses give you a system that's quietly wrong. The sentinel gives you one that tells you it's wrong.`})]}),(0,t.jsxs)(r,{title:`The JSON contract, the round trip, and negotiating a loop I didn't own`,summary:`One operation per invocation, why a successful send isn't the same as the vendor existing, and being wrong out loud`,minutes:2,children:[(0,t.jsx)(n.p,{children:`Each operation, create and update and block, has its own JSON payload shape. The controller creates a shell vendor record first, purely to obtain an ID before the round trip, then calls a subflow that builds and sends exactly one payload per invocation. A replacement sends a create for the new vendor, then a second call updating the old one with a block and a note carrying the new ERP ID. One operation per invocation is what makes retry and escalation tractable.`}),(0,t.jsx)(n.p,{children:`After a package goes out, the subflow validates that the expected change actually came back within a turnaround threshold, and opens an error handling task if it didn't. A successful send isn't the same as the vendor existing.`}),(0,t.jsx)(n.p,{children:`I couldn't get the owning teams to describe their own segment from a blank page. So I drew the whole loop, with directions, frequencies, and my best guess at each handoff, and let each team correct their piece. Being visibly wrong is cheaper than asking an open question, and it gave people something to react to, which was the only mode this group had.`})]})]}),`
`,(0,t.jsxs)(c,{id:`why`,title:`Why it was the right call`,children:[(0,t.jsx)(n.p,{children:`Without a dictionary, you can't design once and trust it.`}),(0,t.jsx)(s,{children:`The cascade, the sentinel, the delta property, and the return validation are all the same move: assume the data will disagree with the design, and make the disagreement visible instead of fatal.`}),(0,t.jsx)(n.p,{children:`It is the only responsible posture when nobody can tell you what the data looks like. Read-only by default is what made it safe to hold a large copy of data we didn't own.`})]}),`
`,(0,t.jsxs)(c,{id:`wrong`,title:`When this would be the wrong call`,children:[(0,t.jsx)(n.p,{children:`If a client has a maintained data dictionary and one stable source of truth, most of this machinery is waste. Coalesce on the key everybody already agrees on and move on.`}),(0,t.jsx)(r,{title:`The sentinel is the one to be careful with`,summary:`When a data quality queue becomes worse than the nulls it replaced`,minutes:1,children:(0,t.jsx)(n.p,{children:`The sentinel creates a data quality queue somebody has to work, and if nobody does, it's a table that grows forever while looking like the problem is handled, which is worse than nulls. Only build it if you can name the person whose job includes that report. Same for the shell record: if the downstream system accepts a correlation ID you generate, skip it.`})})]}),`
`,(0,t.jsx)(n.hr,{}),`
`,(0,t.jsx)(n.p,{children:(0,t.jsxs)(n.em,{children:[`My part, specifically: the data model and the import pattern. The development team implemented and extended it from there. Engagement status is on the `,(0,t.jsx)(n.a,{href:`/vendor-management`,children:`project page`}),`.`]})})]})}function r(e={}){let{wrapper:r}=e.components||{};return r?(0,t.jsx)(r,{...e,children:(0,t.jsx)(n,{...e})}):n(e)}function i(e,t){throw Error(`Expected `+(t?`component`:`object`)+" `"+e+"` to be defined: you likely forgot to import, pass, or provide it.")}export{r as default};