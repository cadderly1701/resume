import{t as e}from"./index-CUcsWBF9.js";var t=e();function n(e){let n={a:`a`,code:`code`,em:`em`,hr:`hr`,li:`li`,p:`p`,ul:`ul`,...e.components},{Expand:r,Hero:a,LineDrawing:o,Pull:s,Section:c,TwoLevels:l}=n;return r||i(`Expand`,!0),a||i(`Hero`,!0),o||i(`LineDrawing`,!0),s||i(`Pull`,!0),c||i(`Section`,!0),l||i(`TwoLevels`,!0),(0,t.jsxs)(t.Fragment,{children:[(0,t.jsx)(a,{eyebrow:`Vendor management · Sample 03`,title:(0,t.jsx)(t.Fragment,{children:`What do you do when a legitimate design requires 7500 variables?`}),subtitle:`Generated in an hour instead of configured over weeks, then maintained by rules rather than by hand.`,side:`right`,children:(0,t.jsx)(o,{name:`form-at-scale`,alt:`A dialog box reading New Variable, repeated and fanned back into the distance.`})}),`
`,(0,t.jsx)(n.p,{children:`Roughly 80 vendor attributes, four stored values each, on a form non-developers maintain.`}),`
`,(0,t.jsx)(l,{shortMinutes:4,detailMinutes:16}),`
`,(0,t.jsxs)(c,{id:`problem`,title:`The problem`,children:[(0,t.jsx)(n.p,{children:`Vendor onboarding and vendor change, delivered as catalog items that all collect the same attribute set: legal name, tax ID, remit-to address, payment terms, banking details, category code, tax forms and certificates of insurance, and a couple dozen more finance and compliance fields.`}),(0,t.jsx)(n.p,{children:`We weren't the only thing writing to those records. Direct access to the ERP wasn't being switched off on the same timeline, and neither was API access from other tools, and requests stay open long enough for the data underneath them to move.`}),(0,t.jsx)(n.p,{children:`So a change request can't just collect a new value. For one attribute you have to know four things: what the record holds right now, what it held when the request was submitted, what the requester asked for, and what the support team is actually going to send.`}),(0,t.jsx)(n.p,{children:`Repeated across roughly 80 attributes, plus the two-column containers each one renders inside, that's what puts the variable set near 500. The approach on the table was several weeks of manual configuration, then thousands of UI Policy conditions to decide which variables a given user sees.`}),(0,t.jsxs)(r,{title:`Why one attribute needs four values`,summary:`The race condition underneath all of it, and the layout rows everyone forgets when they estimate`,minutes:3,children:[(0,t.jsx)(n.p,{children:`Downstream those values become a vendor master record in one of the ERP platforms.`}),(0,t.jsx)(n.p,{children:`Here's the catch, and it's what makes this hard rather than just big. We were building the system of engagement, but direct access to the ERP wasn't being switched off on the same timeline, and neither was API access from other tools. So we couldn't assume we were the only thing changing a vendor record. The process is time-sensitive too, and requests stay open long enough for the data underneath them to move.`}),(0,t.jsx)(n.p,{children:`That means a change request can't just collect a new value. For one attribute you have to know four things: what the record holds right now, what it held when the request was submitted, what the requester asked for, and what the support team is actually going to send. The last two are genuinely different. The support team routinely reformats values to satisfy character and formatting rules in the ERP, so a correct confirmed value often isn't the string the requester typed.`}),(0,t.jsx)(n.p,{children:`So each attribute isn't a field, it's a small family of them:`}),(0,t.jsxs)(n.ul,{children:[`
`,(0,t.jsx)(n.li,{children:`System Current: what the vendor record holds when the form loads.`}),`
`,(0,t.jsx)(n.li,{children:`System Current at Time of Request: the snapshot from submission, so you catch a value that moved underneath an in-flight request.`}),`
`,(0,t.jsx)(n.li,{children:`Requested Left / Requested Right: the requester's value in two layout positions that stay synchronized, so it renders on either side of a comparison without being stored twice.`}),`
`,(0,t.jsx)(n.li,{children:`Confirmed: what the fulfiller commits to sending.`}),`
`,(0,t.jsx)(n.li,{children:`Container Start / Container Split / Container End: the two-column wrapper each attribute renders inside. No data, pure layout, and three more rows to create for every attribute in the set.`}),`
`]}),(0,t.jsx)(s,{children:`The containers are the part people forget when they estimate this. They carry no information at all and they're a third of the build.`})]})]}),`
`,(0,t.jsxs)(c,{id:`built`,title:`What I built`,children:[(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:`sc_item_option_new`}),` is a table and variables are rows in it, so I generated all 500 from the requirements spreadsheet through an import set and transform map. About an hour, mapping fixes included, against an estimate measured in weeks.`]}),(0,t.jsx)(n.p,{children:`Behavior runs off six handling patterns rather than a rule per field, one pattern assigned per attribute per stage, which turned requirements gathering into a spreadsheet: rows are attributes, columns are stages, one word per cell.`}),(0,t.jsx)(n.p,{children:`Every catalog item then gets a Catalog Item Configuration record where data admins pick attributes into list collectors, and a business rule expands those picks into the variable name lists the client scripts iterate. onLoad reads the configuration record exactly once into a hidden variable on the form, and every check after that runs client side.`}),(0,t.jsxs)(r,{title:`Treating variable creation as a data load`,summary:`Why the spreadsheet became the spec, and why speed was the smallest part of the payoff`,minutes:2,children:[(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:`sc_item_option_new`}),` is a table. Variables are rows in it. Once you say that out loud, building 500 of them stops being configuration work and becomes an import.`]}),(0,t.jsxs)(n.p,{children:[`The attribute list already existed as a requirements spreadsheet. I expanded each attribute row into its variant rows with sheet formulas, following a strict naming convention (`,(0,t.jsx)(n.code,{children:`u_current_X`}),`, `,(0,t.jsx)(n.code,{children:`u_requested_left_X`}),`, `,(0,t.jsx)(n.code,{children:`u_requested_right_X`}),`, `,(0,t.jsx)(n.code,{children:`u_confirmed_X`}),`), generated the container rows alongside them, then loaded the result through an import set and transform map into the variable set. About an hour, mapping fixes included, against an estimate measured in weeks.`]}),(0,t.jsx)(n.p,{children:`Speed was the smallest part of the payoff. Naming came out mechanically consistent, so every downstream script could derive an attribute's siblings from its name instead of consulting a lookup table. Rebuilding was cheap: change the sheet, re-run the transform. And the spreadsheet was the spec, so what got built matched what was asked for by construction rather than by review. The same instinct showed up later for approval step and override configuration, loaded through import set and transform map with coalesce keys and record-count validation instead of typed in by hand.`})]}),(0,t.jsxs)(r,{title:`Naming the patterns before building any behavior`,summary:`Six patterns instead of a rule per field, and the part everyone kept getting wrong`,minutes:2,children:[(0,t.jsx)(n.p,{children:`Before behavior got built, I wrote a handling pattern explainer. Not a list of rules, a set of patterns that produce them. Six exist: Read Only, Mandatory, Editable, Hide, Default Read Only, Default Editable. Each one resolves to a visible / editable / mandatory triple. Each stage independently assigns one pattern per attribute.`}),(0,t.jsx)(n.p,{children:`Then the part everyone kept getting wrong: which members of that family actually render at a given stage. Tax ID marked Mandatory at fulfiller ERP means System Current read-only, Requested Left read-only, Confirmed mandatory, Requested Right hidden, and System Current at Time of Request shown only when it differs from System Current. Same attribute, same word in the cell, completely different form at the requester stage.`}),(0,t.jsx)(n.p,{children:`Documenting that turned requirements gathering into a spreadsheet: rows are attributes, columns are stages, each cell is one of six words. Stakeholders could fill that in, and they did. Nobody had to describe UI behavior in prose again.`})]}),(0,t.jsxs)(r,{title:`Making runtime behavior a lookup instead of a rule per field`,summary:`One server call on load, then everything client side, and what a naming convention buys at eighty attributes`,minutes:3,children:[(0,t.jsxs)(n.p,{children:[`Every catalog item gets a Catalog Item Configuration record. Data admins pick attributes into eight list collectors, one per requester or fulfiller context, and they only ever pick the `,(0,t.jsx)(n.code,{children:`u_current_X`}),` variable. They're selecting attributes, not fields.`]}),(0,t.jsxs)(n.p,{children:[`An after insert/update business rule expands those selections into comma-separated variable name lists stored on the config record. It derives the variants from each name, applies the requester rules (New maps to the left requested variant, Change maps to the right), applies the fulfiller rules per phase, then normalizes each list: trim, dedupe, sort, write only on change so the audit stays readable. One `,(0,t.jsx)(n.code,{children:`IN`}),` query against `,(0,t.jsx)(n.code,{children:`item_option_new`}),` builds the name map, so no per-row queries. A name that breaks the convention gets skipped and logged rather than throwing.`]}),(0,t.jsxs)(n.p,{children:[`Client scripts apply `,(0,t.jsx)(n.code,{children:`setDisplay`}),`, `,(0,t.jsx)(n.code,{children:`setMandatory`}),`, and `,(0,t.jsx)(n.code,{children:`setReadOnly`}),` by iterating those name lists. Runtime does no reasoning. It reads a decision made at save time, server side, so Platform UI, Workspace, and API paths all agree.`]}),(0,t.jsx)(n.p,{children:`And it reads that decision exactly once. onLoad makes a single call to the configuration record, copies the derived lists into a hidden variable on the form, and every check after that runs against the hidden variable, entirely client side. The form still behaves dynamically, with fields appearing and disappearing as the requester makes choices, and none of that costs a server round trip. On a form carrying 500 variables that's the difference between a page that responds and a page people describe as slow, which is the kind of complaint that gets a good design abandoned.`}),(0,t.jsx)(n.p,{children:`Everything on top reads the same structure by naming convention rather than hard-coded field names: hide a Requested field when it and its System Current partner are both empty, show the "Value Changed" message only when System Current differs from Confirmed and only as the process type calls for, raise an error when System Current moved since submission.`}),(0,t.jsx)(n.p,{children:`That last one is the race-condition guard, and it only works because it's generic. All of this derives from the naming convention and the configuration record, so every one of the roughly 80 attributes behaves identically. Same rules, same ordering, same messages, same drift warning. Someone who learns how one field behaves has learned all of them, and a support agent who spots stale data on one knows exactly where to look on the rest.`})]})]}),`
`,(0,t.jsxs)(c,{id:`why`,title:`Why it was the right call`,children:[(0,t.jsx)(n.p,{children:`Changing whether a certificate of insurance is mandatory at approval is a list collector edit and a save by a data admin, not a story and a release.`}),(0,t.jsx)(n.p,{children:`And the four-value family is what lets the form do the fulfiller's subtraction for them. An attribute is ignorable when the requester didn't ask to change it and it hasn't moved in the source system since submission, which the form can compute instead of a person checking, with eighty attributes in front of them and a queue behind them.`}),(0,t.jsx)(s,{children:`Hiding a field is usually a cosmetic decision. Here it was the safety feature, because attention is finite and the failure mode is someone scrolling past the one row that mattered.`}),(0,t.jsxs)(r,{title:`The reasoning in full`,summary:`Three constraints that compound, and the one I'd point at if I only got one`,minutes:4,children:[(0,t.jsx)(n.p,{children:`First, four values per attribute is a data model, not a user interface. The same information has to serve two people who need opposite things from it. A requester in the portal should see something simple: here's what we have, here's what you'd like it to be. Nobody changing a remit-to address should have to read a four-column audit grid. The support team working that same request at a task has the opposite problem, and it's subtraction rather than addition. Eighty attributes is far too many to review one at a time on every request, so their real question is what they can safely ignore. An attribute is ignorable when the requester didn't ask to change it and it hasn't moved in the source system since submission. Everything else needs a person to look at it.`}),(0,t.jsx)(n.p,{children:`So the form has to answer three questions on sight. Where did the requester ask for a change? Where has a value moved underneath the request since it was submitted? And where would sending the confirmed value quietly overwrite a change somebody else made in the meantime? That last one is the race condition, and it's the expensive one, because the failure mode is the system of record ending up with an older value than it held before the request was worked.`}),(0,t.jsx)(n.p,{children:`Second, the approach on the table was several weeks of manual configuration. Someone sits in the variable set editor building 500 variables one at a time, matching label, internal name, type, order, and choice list against a spreadsheet. Weeks of clicking, a guaranteed typo rate, and no way to diff what got built against what was specified.`}),(0,t.jsx)(n.p,{children:`Third, and worst: once all 500 exist, somebody still has to decide which ones a given user sees. Behavior varies by catalog item, by process type (New vendor vs Change vendor), by stage (requester, fulfiller initial, approval, fulfiller ERP, post tasks), and by persona. Tax ID might be mandatory for the requester, read-only at approval, and mandatory again for the fulfiller finalizing the ERP payload. As UI Policies that's thousands of conditions, the same weeks-of-clicking problem in a different hat, except this one comes back every time the business changes its mind.`}),(0,t.jsx)(n.p,{children:`The client had also asked that data admins maintain the process without a developer, so UI Policies weren't just expensive to build. They put every future change behind a release.`}),(0,t.jsx)(n.p,{children:`Which is why this fit these circumstances:`}),(0,t.jsxs)(n.ul,{children:[`
`,(0,t.jsx)(n.li,{children:`The maintenance profile matched the ask. Changing whether a certificate of insurance is mandatory at approval is a list collector edit and a save by a data admin, not a story and a release.`}),`
`,(0,t.jsx)(n.li,{children:`The scale was past the crossover. 500 variables across five stages and two process types isn't a UI Policy problem, and pretending otherwise moves the cost to whoever inherits it.`}),`
`,(0,t.jsx)(n.li,{children:`The client needed to see it work. Handing them a catalog item where flipping one config field visibly changed the form did in ten minutes what a requirements workshop hadn't.`}),`
`,(0,t.jsx)(n.li,{children:`The requirements artifact and the build ended up the same shape. The matrix the business filled out is what the config record stores.`}),`
`]}),(0,t.jsx)(n.p,{children:`The subtraction is the part I'd point at if I only got one. Storing the value as it stood at submission looks like redundancy right up until a fulfiller has eighty attributes in front of them and a queue behind them.`})]})]}),`
`,(0,t.jsxs)(c,{id:`wrong`,title:`When this would be the wrong call`,children:[(0,t.jsx)(n.p,{children:`Most of the time, honestly. A form with 30 fields and requirements that hold still should use UI Policies, full stop. At that size this design is actively harmful: you've put an indirection layer, a business rule, a naming convention, and a client script between "make this field mandatory" and the field being mandatory. Debugging a UI Policy is one click. Debugging this means reading a derived comma-separated string and working out which variant should have landed in it.`}),(0,t.jsxs)(r,{title:`Where the crossover is`,summary:`The two conditions that have to hold together, and the discipline it costs forever`,minutes:1,children:[(0,t.jsx)(n.p,{children:`The crossover point, plainly: build it this way when the behavior decisions (fields times contexts) run into the hundreds and non-developers have to change them without a release. Both true, the indirection pays for itself fast. One true, think hard. Neither, don't.`}),(0,t.jsx)(n.p,{children:`It also costs discipline forever, because it works only while the naming convention holds. A variable created by hand that breaks it silently participates in nothing, which is why the derivation logs skipped names instead of failing quietly.`})]})]}),`
`,(0,t.jsx)(n.hr,{}),`
`,(0,t.jsx)(n.p,{children:(0,t.jsxs)(n.em,{children:[`My part, specifically: the catalog item configuration table, the business rule deriving the visibility, editability, and read-only name lists, the vendor detail variable set built out in full, the import-set generation of the variables, and the onLoad and onChange client scripts putting the right variables on the right item at the right time, maintainable end to end without admin rights. The development team implemented and extended it from there. Engagement status is on the `,(0,t.jsx)(n.a,{href:`/vendor-management`,children:`project page`}),`.`]})})]})}function r(e={}){let{wrapper:r}=e.components||{};return r?(0,t.jsx)(r,{...e,children:(0,t.jsx)(n,{...e})}):n(e)}function i(e,t){throw Error(`Expected `+(t?`component`:`object`)+" `"+e+"` to be defined: you likely forgot to import, pass, or provide it.")}export{r as default};