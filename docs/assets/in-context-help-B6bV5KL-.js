import{t as e}from"./index-lDt8LPzc.js";var t=e();function n(e){let n={a:`a`,code:`code`,em:`em`,hr:`hr`,p:`p`,strong:`strong`,...e.components},{Expand:r,Hero:a,LineDrawing:o,Pull:s,Section:c,TwoLevels:l}=n;return r||i(`Expand`,!0),a||i(`Hero`,!0),o||i(`LineDrawing`,!0),s||i(`Pull`,!0),c||i(`Section`,!0),l||i(`TwoLevels`,!0),(0,t.jsxs)(t.Fragment,{children:[(0,t.jsx)(a,{eyebrow:`Vendor management · Sample 08`,title:`How do you help someone who's stuck on one field?`,subtitle:`A reusable widget that launches contextual help at the exact step, and the platform gotcha that forced three rewrites.`,side:`right`,children:(0,t.jsx)(o,{name:`in-context-help`,alt:`A speech bubble with the word help written inside it.`})}),`
`,(0,t.jsx)(n.p,{children:`A button next to one field, and what it took to render it.`}),`
`,(0,t.jsx)(l,{shortMinutes:2,detailMinutes:7}),`
`,(0,t.jsxs)(c,{id:`problem`,title:`The problem`,children:[(0,t.jsx)(n.p,{children:`The vendor forms here are long, and most people filling them out do it a few times a year. A guided tour covering the whole form was already in scope.`}),(0,t.jsx)(n.p,{children:`Trouble is, nobody's ever stuck on the whole form. They're stuck on one field.`}),(0,t.jsx)(s,{children:`Sitting through every earlier step to reach the one about tax ID is worse than no help at all, and it's why tours get closed in two seconds.`}),(0,t.jsx)(n.p,{children:`So the ask turned narrower: a small help button next to the field that jumps the tour to the step explaining it. Same content, entered where the trouble is.`})]}),`
`,(0,t.jsxs)(c,{id:`built`,title:`What I built`,children:[(0,t.jsx)(n.p,{children:`I built a Service Portal widget, dropped onto the form through a custom variable, with everything placement-specific in instance options: the tour, the step to jump to, and the button label. One widget serves every tour and every step, and several instances can sit on the same page pointed at different places.`}),(0,t.jsx)(n.p,{children:`It resolves the guided tour service by probing candidate service names and falls back to a URL parameter when none of them answer, and where the platform already has a control that does the job, it triggers that rather than reimplementing it.`}),(0,t.jsx)(n.p,{children:`It worked, and it ran in the portal, which is where the earlier attempts didn't.`}),(0,t.jsxs)(r,{title:`The three rewrites, and the platform gotcha behind them`,summary:`Why the obvious answer renders in the platform and not in the portal, plus the catalog client script rules that each cost a cycle`,minutes:3,children:[(0,t.jsx)(n.p,{children:`The obvious platform answer doesn't work here. A Macro variable, or "Macro with label", renders a button perfectly well in the platform view and not at all in the portal, because the portal builds catalog forms out of widgets instead of Jelly. That one costs people an afternoon regularly, and it cost me one.`}),(0,t.jsx)(n.p,{children:`From there I tried an HTML variable, injecting the button from an onLoad catalog client script, and a formatter. Each has its own tax: sanitizers and theme spacing, insertion points and render timing, lost label control.`}),(0,t.jsxs)(n.p,{children:[`Catalog client scripts have their own rules too. Optional chaining won't save. A promise `,(0,t.jsx)(n.code,{children:`.catch()`}),` won't save. A plain `,(0,t.jsx)(n.code,{children:`document.getElementById`}),` came back null at runtime and had to be reached through `,(0,t.jsx)(n.code,{children:`window.document`}),`. None of those are hard once you know them, and all of them cost a cycle when you don't.`]}),(0,t.jsx)(n.p,{children:`Then the moving part: the client-side service token for guided tours isn't stable across releases, so writing one service name into a script is writing a future upgrade defect.`}),(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.strong,{children:`Resolving the tour service.`}),` On click, the widget pulls the Angular injector off the page, probes a list of candidate service names, and calls the first that resolves, passing the target step. If none resolve, it appends the tour URL parameter and reloads, starting the tour at step one. Worse than the API path, much better than a dead button.`]}),(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.strong,{children:`Instance options instead of hard-coded values.`}),` Tour id, target step, and button label all come in as options, so one widget serves every tour and every step, and several instances can sit on one page pointed at different steps. Moving it to another item is configuration, not code.`]})]}),(0,t.jsxs)(r,{title:`Triggering the platform's own control instead of rebuilding it`,summary:`A second use of the same button, three problems that disappeared at once, and tolerating input the caller will get wrong`,minutes:2,children:[(0,t.jsx)(n.p,{children:`A second use of the same button opens the native record preview for a reference variable, and I didn't rebuild that modal. The widget finds the variable's own preview control and clicks it, which gets the built-in behavior exactly. Calling the underlying function directly produced the wrong modal, tripped Angular's digest guard, and needed a view string that wasn't reliably on the scope. Clicking the real control made all three go away.`}),(0,t.jsxs)(n.p,{children:[`To find that control, the widget walks the rendered lookup buttons and matches on the bare field name off each one's scope, normalizing the option value first so `,(0,t.jsx)(n.code,{children:`vendor`}),` and `,(0,t.jsx)(n.code,{children:`variables.vendor`}),` both work. The first live failure was that exact mismatch, and the fix belonged in the widget, not in a note telling everyone which form to type.`]}),(0,t.jsx)(n.p,{children:`The fallbacks exist because the foundation moves. Probing service names and degrading to a URL parameter is what makes it survivable on an instance whose release you don't control.`}),(0,t.jsx)(n.p,{children:`A conversation about targeted in-line help wasn't going to land, so I built the button instead of describing it.`})]})]}),`
`,(0,t.jsxs)(c,{id:`why`,title:`Why it was the right call`,children:[(0,t.jsx)(n.p,{children:`The tour content already existed and was already maintained. This adds entry points rather than a second body of help text that drifts. A visible button doing an obvious thing settled a discussion words weren't settling.`}),(0,t.jsx)(s,{children:`Help should begin where someone is stuck, not at the beginning.`})]}),`
`,(0,t.jsxs)(c,{id:`wrong`,title:`When this would be the wrong call`,children:[(0,t.jsx)(n.p,{children:`This leans on client-side internals that aren't part of any supported contract: an Angular injector, service names that change between releases, and the DOM structure of the rendered form. That's precisely what an upgrade breaks. The layered fallbacks aren't belt and braces, they're an admission that the foundation is release-sensitive.`}),(0,t.jsx)(n.p,{children:`So the test is whether there's a real adoption problem. A form people struggle with, where help at the right field changes completion rates, earns this. Decoration doesn't. And a shop that won't put "retest the help buttons" on its upgrade checklist shouldn't build it, because the failure mode is silent: the button still renders, it just stops doing anything useful.`})]}),`
`,(0,t.jsx)(n.hr,{}),`
`,(0,t.jsx)(n.p,{children:(0,t.jsxs)(n.em,{children:[`My part, specifically: I built the widget myself and got it to a reusable state, with instance options for tour, step, and label, the service probe with URL-parameter fallback, and the variable targeting with name normalization. It ran in the portal, which is where the earlier attempts did not. Engagement status is on the `,(0,t.jsx)(n.a,{href:`/vendor-management`,children:`project page`}),`.`]})})]})}function r(e={}){let{wrapper:r}=e.components||{};return r?(0,t.jsx)(r,{...e,children:(0,t.jsx)(n,{...e})}):n(e)}function i(e,t){throw Error(`Expected `+(t?`component`:`object`)+" `"+e+"` to be defined: you likely forgot to import, pass, or provide it.")}export{r as default};